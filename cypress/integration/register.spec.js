describe('Test register page', () => {
  it('submits form and succeeds', () => {
    cy.visit("/register");
    // to do
  });

  it('tries to submit but fails validation', () => {
    cy.visit("/register");
    // to do
  });

  it('submits register form and fails', () => {
    cy.visit("/register");
    // to do
  });

  it('redirects logged user to dashboard', () => {
    cy.visit("/register");
    // to do
  });
});
